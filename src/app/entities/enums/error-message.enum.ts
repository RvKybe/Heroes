/**
 * Enum для отображения сообщения ошибок
 *
 * @param HERO_EXIST - герой существует
 * @param ABILITY_EXIST - способность существует
 */
export enum EErrorMessage {
    HERO_EXIST = 'Такой герой уже существует',
    ABILITY_EXIST = 'Такая способность уже существует'
}